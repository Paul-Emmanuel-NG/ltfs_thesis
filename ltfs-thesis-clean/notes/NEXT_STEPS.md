# Next steps

1. Make `sim/core/` scripts portable by removing hard-coded absolute paths.
2. Ensure `mp_freeflow.py` produces both `freeflow_mp.csv` and `freeflow_ltfs.csv`.
3. Repair `sweep_ltfs_params.py` imports and filename assumptions.
4. Rerun baseline and LTFS once cleanly.
5. Generate summary tables and plots for thesis Results and Discussion.
